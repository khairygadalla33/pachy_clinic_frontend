server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied expired no-cache no-store private auth;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml application/javascript;

    # Proxy for Deboura store Next.js assets
    location /_next/ {
        proxy_pass https://deboura.com/_next/;
        proxy_set_header Host deboura.com;
        proxy_ssl_server_name on;
        proxy_hide_header X-Frame-Options;
        proxy_hide_header Content-Security-Policy;
        proxy_hide_header content-security-policy;
        proxy_hide_header x-frame-options;
        add_header X-Frame-Options "SAMEORIGIN";
    }

    # Proxy for Deboura store cdn-cgi
    location /cdn-cgi/ {
        proxy_pass https://deboura.com/cdn-cgi/;
        proxy_set_header Host deboura.com;
        proxy_ssl_server_name on;
    }

    # Proxy for Deboura store HTML with base href injection and header stripping
    location /deboura-store/ {
        proxy_pass https://deboura.com/;
        proxy_set_header Host deboura.com;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_ssl_server_name on;
        
        proxy_hide_header X-Frame-Options;
        proxy_hide_header Content-Security-Policy;
        proxy_hide_header content-security-policy;
        proxy_hide_header x-frame-options;
        add_header X-Frame-Options "SAMEORIGIN";

        proxy_set_header Accept-Encoding "";
        sub_filter '<head>' '<head><base href="https://deboura.com/">';
        sub_filter_once on;
    }

    # Cache static assets for Pachy Clinic
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, no-transform";
    }

    # SPA routing - redirect all non-file requests to index.html
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-store, no-cache, must-revalidate";
    }
}
